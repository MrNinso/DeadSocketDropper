# DeadSocketDropper
A Go application designed to monitor, track, and automatically terminate hanging or long-lived TCP connections on a Linux host using ss --kill. Runs efficiently as a Docker container.
